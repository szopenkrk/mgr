{% extends "MyWorkBundle::layout.html.twig" %}

{% block body %}
    {% block container %}
        Panel podglądu dostępnych użytkowników <br /><br />
            <table>
                <thead>
                <tr>

                    <th>Login</th>
                    <th>Role Użytkownika</th>
                </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr>

                        <th>{{ entity.username }}</th>
                        <th>Autor Edytor Recenzent</th>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
    {% endblock %}
{% endblock %}